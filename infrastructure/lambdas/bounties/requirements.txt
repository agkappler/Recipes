# Runtime dependencies for this Lambda deployment package.
# Pin 1.1.0: same `from ulid import ULID` API, pure Python (no native wheels). python-ulid 3.x pulls
# typing_extensions and is awkward with `pip -t` + arm64 if Docker bundling uses the wrong platform.
python-ulid==1.1.0
