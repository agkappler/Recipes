# Runtime dependencies for this Lambda deployment package.
# Same module (`from ulid import ULID`) from 1.x through 3.x; Lambda (3.12) resolves latest, older local Pythons cap at 1.1.0.
python-ulid>=1.1.0
